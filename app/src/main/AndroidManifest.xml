<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.zzzz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".zzzzActivity"
            android:label="@string/app_name" />
        <activity android:name=".ButtonAlignActivity" />
        <activity android:name=".PrintStarActivity" />
        <activity android:name=".ButtonAlign2Activity" />
        <activity android:name=".HomePageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".CalCActivity" />
        <activity android:name=".MP3PlayerActivity"/>

    </application>

</manifest>